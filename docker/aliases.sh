#!/bin/bash

alias docker="sudo docker"
alias docker-compose="sudo docker-compose"

alias lazydocker="cd ~/docker/lazydocker && docker-compose run --rm --name lazydocker lazydocker"
alias excalidraw="cd ~/docker/excalidraw && docker-compose up -d"

